let numbers = [10, 20, 30, 40, 50];

//let passing = numbers.filter((x) => x > 25);
//console.log(passing);

for (let i = 0; i < numbers.length; i++) {
  if (numbers[i] > 25) {
    console.log(numbers[i]);
  }
}
